# .shellcheckrc
# ShellCheck configuration for claude-idd-framework

# Shell dialect (bash, sh, dash, ksh)
shell=bash

# Disable specific warnings (space-separated list of SC codes)
# Example: disable=SC2034,SC2154

# Enable optional checks (space-separated list)
# enable=all

# Source path for sourced files
source-path=SCRIPTDIR

# Exclude paths (glob patterns)
# exclude-path=vendor/*
exclude-path=*.md,node_modules/**

# Set severity threshold (error, warning, info, style)
# severity=style
severity=warning
